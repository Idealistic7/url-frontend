// App.js (or any other component)

import React from 'react';
import UrlShortenerForm from './components/UrlShortenerForm'; // Adjust the path as needed

const App = () => {
  return (
    <div className="App">
      <h1>URL Shortener</h1>
      <UrlShortenerForm />
      {/* Other components or content */}
    </div>
  );
};

export default App;
